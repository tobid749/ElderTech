@model Eldertech.Models.Aplicacion
@{
    ViewData["Title"] = Model.Nombre;
    var preguntas = ViewBag.Preguntas as List<Eldertech.Models.PreguntaAplicacion>;
}
<header>
  <nav>
    <div class="nav-left">
      <a href='@Url.Action("IndexSesionado", "Home")' aria-label="Inicio ElderTech" class="brand">
        <img src="/Imagenes/Logo.png" alt="Logo ElderTech" class="logo">
      </a>

      <form action='@Url.Action("Articulos", "Home")' method="get" class="search-form" role="search">
        <input type="search" name="q" placeholder="Buscar artículos" aria-label="Buscar artículos">
        <button type="submit" class="search-btn">🔍</button>
      </form>
    </div>

    <ul class="nav-links">
      <li><a href='@Url.Action("Articulos", "Home")'>Articulos</a></li>
      <li><a href='@Url.Action("Camino", "Home")'>Camino</a></li>
      <li><a href='@Url.Action("Aplicaciones", "Home")' style="background-color: #008D36;">Aplicaciones</a></li>
      <li><a href='@Url.Action("ForoSesionado", "Home")'>Foro</a></li>
      <li><a href='@Url.Action("Contacto", "Home")'>Contacto</a></li>
    <li><a href='@Url.Action("NecesitoAyuda", "Home")'>Necesito Ayuda</a></li>
    </ul>
  </nav>
</header>
<section class="banner-eldertech">
    <h1 class="banner-title">@Model.Nombre</h1>
</section>

<main class="aplicacion-contenedor">

    <div class="aplicacion-content">

        <div class="aplicacion-img-container">
            <img src="@Model.Foto" alt="@Model.Nombre" class="aplicacion-img">
        </div>

        <div class="aplicacion-preguntas">
            <h2 class="preguntas-title">Preguntas</h2>

          @{
    var pregunta = ViewBag.PreguntaActual as Eldertech.Models.PreguntaAplicacion;
    var opciones = ViewBag.OpcionesMezcladas as List<(string texto, int num)>;
}

@if (pregunta != null && opciones != null)
{
    <div class="pregunta" data-correcta="@pregunta.Correcta">

        <p class="pregunta-text">@pregunta.Enunciado</p>

        <div class="respuesta-grid">
            @foreach (var op in opciones)
            {
                <button data-num="@op.num">@op.texto</button>
            }
        </div>

        <form method="post" asp-action="Siguiente" asp-route-id="@Model.IDAplicacion" asp-route-next="@ViewBag.NextIndex">
            <button id="btnSiguiente" class="btn-siguiente" disabled>Siguiente ➜</button>
        </form>
    </div>
}
else
{
    <p class="sinPreguntas">Muy pronto vas a poder practicar con preguntas de esta aplicación ✅</p>
}


        </div>
    </div>

    <h2 class="articulos-title">Artículos relacionados</h2>

    <div class="articulos-rel">
        <!-- Aquí luego integramos Artículos desde DB -->
        <p style="color:#777; text-align:center;">Muy pronto disponibles ✅</p>
    </div>

</main>




<script>
@section Scripts {
<script>
document.addEventListener("DOMContentLoaded", function () {
    const preguntas = document.querySelectorAll('.pregunta button[data-num]');
    if (!preguntas.length) return;

    preguntas.forEach(btn => {
        btn.addEventListener('click', () => {
            const cont = btn.closest('.pregunta');
            const correcta = cont.dataset.correcta;
            const botones = cont.querySelectorAll('button[data-num]');
            const nextBtn = document.getElementById("btnSiguiente");

            // Crear o seleccionar el mensaje
            let msg = cont.querySelector('.resultado-msg');
            if (!msg) {
                msg = document.createElement('p');
                msg.classList.add('resultado-msg');
                cont.appendChild(msg);
            }

            // Desactivar botones y colorear
            botones.forEach(b => {
                b.disabled = true;
                if (b.dataset.num === correcta) {
                    b.classList.add('correcta');
                } else if (b === btn) {
                    b.classList.add('incorrecta');
                }
            });

            // Mostrar mensaje
            if (btn.dataset.num === correcta) {
                msg.textContent = "¡Correcto! ✅";
                msg.className = "resultado-msg correcto-msg";
            } else {
                msg.textContent = "Ups, no era esa 😅";
                msg.className = "resultado-msg incorrecto-msg";
            }

            msg.style.opacity = "1";

            // Desaparecer después de 1.5s y habilitar siguiente
            setTimeout(() => {
                msg.style.opacity = "0";
                if (nextBtn) nextBtn.disabled = false;
            }, 1500);
        });
    });
});
</script>
}
